#include "main.h"

/**
 * mul - function that returns mulitiplication of two integer
 * @a: first integer
 * @b: second integer
 *
 * Return: 0
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
