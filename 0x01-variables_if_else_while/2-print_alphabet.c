#include <stdio.h>
/**
 * main - Entry point
 * description: prints alphabet in lowercase\n
 * return always 0 (success)
*/

int main(void)
{
	int ch;
	for (ch = 'a'; ch <= 'z'; ch++)

	{
			putchar(ch);
	}
			putchar('\n');

	return 0;
}
